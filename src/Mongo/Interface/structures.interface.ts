export interface InfluenzaInterface {
  AMB: {
    AMB: string;
    CONV: string;
    DISTRIBUICAOHUMANO: string;
  };
  HUMANO: {
    INI: string;
    MOV: string;
    TRA: string;
  };
  SIMULACAO: {
    SIM: string;
  };
}
